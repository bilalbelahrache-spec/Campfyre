[Unit]
Description=Campfire coordinator
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/campfire-coordinator
ExecStart=/usr/bin/node server.js
Restart=always
RestartSec=5
Environment=PORT=8080
User=campfire

[Install]
WantedBy=multi-user.target
