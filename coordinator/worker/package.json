{
  "name": "campfyre-coordinator-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Campfyre coordinator on Cloudflare Workers + Durable Objects",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "test": "node test/test_migration.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "ws": "^8.18.0"
  }
}
